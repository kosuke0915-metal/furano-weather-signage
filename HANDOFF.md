# 富良野市周辺 天気予報サイネージ — 引き継ぎ資料

VS Code + Claude Code での作業引き継ぎ用のドキュメントです。これまでの検討経緯・技術構成・既知の制限をまとめています。

## 1. 概要

- **用途**: デジタルサイネージ（横画面 16:9）での常時表示
- **表示内容**: 富良野市／旭川市／帯広市／札幌市／北見市／函館市の6都市について、9時・12時・15時・18時・21時の気温・天気・降水確率を一覧表示
- **データソース**: [Open-Meteo](https://open-meteo.com/)（無料・APIキー不要の天気予報API）
- **形態**: ビルド不要・フレームワーク不要のHTML（`index.html`＋背景画像`bg-photo.jpg`の2ファイル構成）

## 2. ファイル一覧

| ファイル | 用途 |
|---|---|
| `index.html` | **本番用ファイル** |
| `bg-photo.jpg` | 背景に使用している写真（1920×1080に最適化済み、316KB） |
| `preview.png` | ダミーデータでの表示イメージ（見た目確認用。納品物ではありません） |

**`index.html`と`bg-photo.jpg`は必ず同じフォルダにアップロードしてください。** `index.html`は`bg-photo.jpg`を相対パスで参照する構成になっており（後述の理由によりbase64埋め込みをやめ、通常の画像ファイル参照に変更しました）、この2つが同じディレクトリに無いと背景が表示されません。

## 3. 技術構成

### 3.1 データ取得（WeatherAPI.com）

```
https://api.weatherapi.com/v1/forecast.json
  ?key={APIキー}
  &q={緯度},{経度}
  &days=2
  &aqi=no&alerts=no&lang=ja
```

- **APIキーが必須**です。[WeatherAPI.com](https://www.weatherapi.com/)でアカウント登録後、マイページで発行されるキーを`index.html`内の`WEATHERAPI_KEY`定数（初期値`"YOUR_API_KEY"`）に置き換えてください。このページはバックエンドを持たない静的HTMLのため、キーはクライアント側のJSにそのまま埋め込まれます（ページのソースを見れば誰でも閲覧可能）。サイネージ端末内で完結し外部に公開しない前提であれば実用上大きな問題にはなりにくいですが、留意してください
- 無料プランの上限・利用規約は変更される可能性があるため、商用（店舗・施設等での営業目的の掲示）利用の際は[WeatherAPI.comの料金ページ](https://www.weatherapi.com/pricing.aspx)を必ず確認してください
- `days=2`としているのは、日付境界（24時直前など）で当日分の時刻が取得できないケースを避けるための保険です（Open-Meteo時代の`forecast_days=2`と同じ考え方）
- 天気状況は`condition.code`（[WeatherAPI.comの条件コード表](https://www.weatherapi.com/docs/weather_conditions.json)）で返るため、`index.html`内の`WX_CODES`定数でモノクロSVGアイコンにマッピングしています
- 画面右下（フッター）に`Powered by WeatherAPI.com`のクレジット表記を設置しています（`index.html`内`.credit`）。利用規約変更時は表記要否も再確認してください

### 3.2 対象都市と座標（`index.html`内 `CITIES`定数）

| 都市 | 緯度 | 経度 | 備考 |
|---|---|---|---|
| 富良野市 (Furano) | 43.3416 | 142.3826 | `primary: true`（主要都市として強調表示） |
| 旭川市 (Asahikawa) | 43.7706 | 142.3649 | |
| 帯広市 (Obihiro) | 42.9237 | 143.1962 | |
| 札幌市 (Sapporo) | 43.0618 | 141.3545 | |
| 北見市 (Kitami) | 43.8033 | 143.8907 | |
| 函館市 (Hakodate) | 41.7687 | 140.7291 | |

いずれも市役所付近の概算座標です。特定の観測地点・施設の座標に合わせたい場合はこの配列の`lat`/`lon`を変更してください。都市を追加・削除する場合もこの配列を編集するだけで対応できます（表のグリッドはJSで自動生成されます）。

### 3.3 自動更新

- `REFRESH_MS`定数（現在15分 = `15 * 60 * 1000`）ごとに全都市のデータを再取得
- 時計表示は毎秒更新
- 一部都市の取得に失敗しても他都市の表示は保持し、フッターに「一部取得失敗 Error」と表示。次回の定期更新まで自動リトライは行いません

### 3.4 ローカルキャッシュ（サイネージ切り替え時の空白対策）

サイネージシステムがこの画面に切り替わる／ページが再読み込みされるたびに通信を待つと、数値が表示されるまで一瞬空白になる問題があったため、直近の取得結果を`localStorage`（キー: `furano_weather_cache_v1`）に保存する仕組みを追加しています。

- ページ読み込み直後は、まずこのキャッシュの値を即座に表示（通信を待たない）
- 裏側で最新データを取得し、完了次第そのまま数値を差し替え
- 取得に失敗した都市は空白に戻さず、直前のキャッシュ値を表示し続ける
- 初回アクセス時（そのブラウザ/端末でまだキャッシュが無い状態）のみ、これまで通り初回の通信完了まで「--」表示になります
- `localStorage`はブラウザ（端末）ごとに保存されるため、サイネージ端末のブラウザで一度表示されればそれ以降は空白が発生しません。ブラウザのキャッシュ／サイトデータを消去する設定になっている場合はこの限りではありません

### 3.4 天気アイコン

絵文字ではなく自作のモノクロSVGアイコン（`ICONS`定数: sun/cloud/cloudsun/fog/rain/snow/thunder）を使用しています。絵文字フォントの有無に依存しないため、Linuxベースのサイネージ端末（Raspberry Piなど）でも表示が崩れません。

### 3.5 背景写真の参照方式（画面切り替え時の黒画面対策）

当初は背景写真をbase64化して`index.html`内に埋め込んでいましたが（1ファイルで完結する利便性のため）、これにより`index.html`自体が440KB超の巨大なテキストファイルになり、サイネージがこのページに切り替わる瞬間、ブラウザがそのHTML全体をダウンロード・解析し終えるまで最初の描画ができず、黒画面（または白画面）が数百ms〜1秒程度続く原因になっていました。

そのため、**背景写真は`bg-photo.jpg`という通常の画像ファイルとして分離し、`index.html`からは相対パスで参照する方式に変更しました。** これにより

- `index.html`本体は16KB程度まで軽量化され、レイアウトの骨組みはすぐに解析・描画できるようになった
- `<link rel="preload" as="image" href="bg-photo.jpg">`をhead内に追加し、画像の取得をブラウザに早めに開始させている
- 一度表示された端末では、ブラウザの通常のHTTPキャッシュにより`bg-photo.jpg`が保存されるため、2回目以降の切り替えでは画像の再ダウンロードが発生しない

背景写真を差し替える場合は、新しい写真を16:9（推奨: 1920×1080程度、JPEG品質80前後）にリサイズしたものを`bg-photo.jpg`という同じファイル名で上書きするだけで反映されます（`index.html`側の変更は不要）。**`index.html`と`bg-photo.jpg`は必ず同じフォルダにアップロードしてください。**

## 4. デザイン仕様

- 配色: モノトーン（白・黒・グレーの階調のみ）。CSS変数は`:root`内の`--ink` `--ink-dim` `--ink-faint` `--line` `--panel` `--highlight`などにまとまっています
- レイアウト: 写真背景＋暗めのグラデーションオーバーレイ＋半透明パネル（`backdrop-filter: blur`によるガラス風効果）
- 都市名セル: 中央揃え。日本語名の下に英語表記（ローマ字）を併記。富良野市の行のみ上部に白線＋「MAIN」タグで強調
- 英語表記は「必要な箇所のみ」に絞って追加済み（タイトル副題、都市名、表見出し「CITY」、フッターの降水確率・気温・更新時刻表記）。時刻表記（9時・12時など）は数字のみで国際的に通用するため英訳していません

## 5. 画面切り替え時の黒画面について

サイネージがこのページに切り替わる前後で黒画面が一瞬発生する事象が報告されています。考えられる要因と対応状況は以下の通りです。

1. **HTMLファイル自体の重さによる描画遅延（対応済み）**: 上記3.5の通り、背景写真をbase64埋め込みにしていたことで初回描画が遅れていた可能性が高く、外部ファイル参照＋`preload`に変更しました。2回目以降はブラウザキャッシュも効くため、この要因による遅延はほぼ解消されているはずです。
2. **天気データ取得中の空白表示（対応済み）**: 3.4のローカルキャッシュにより、数値が表示されないまま一瞬空白になる問題は解消済みです。
3. **サイネージ再生ソフト側の切り替え演出（未確認・こちらでは対応不可）**: 複数のコンテンツ（バス時刻表の画像やこのHTMLページなど）を順番に表示する仕組みの場合、コンテンツを切り替える際にソフト側が意図的に一瞬黒画面を挟む仕様になっていることがあります。この場合はHTMLページの中身とは無関係です。**切り分け方法**: 画像スライド同士の切り替わり（例: バス時刻表→別のバス時刻表）でも同様の黒画面が出るかを確認してください。画像同士の切り替えでも黒画面が出るなら、原因はこのHTMLページではなくサイネージ再生ソフト／プレイヤー側の設定（トランジション設定やコンテンツの先読み設定など）にあります。
4. **ブラウザの通常のページ読み込み特性**: サイネージがブラウザで直接URLを順番に開いていくような方式（各コンテンツがそれぞれ別ページ・別タブ相当）の場合、ページの読み込み～描画の間に一瞬何も表示されない空白は、どんなに軽量なページでもゼロにはできません。これを完全に無くすには、サイネージ再生ソフト側で「次のコンテンツを裏で先読みしてからクロスフェードで切り替える」機能が必要です（HTMLページ側の改修では対応できない領域です）。

上記1・2は今回の修正で対応済みです。それでも黒画面が続く場合は、3の切り分け（画像スライド同士でも発生するか）を試していただくのが次の判断材料になります。

**実際に検証した結果、画像スライド同士では黒画面が出ず、このHTMLページ（URL遷移）でのみ発生することが確認されました。** つまり4番目の要因（ブラウザの通常のページ読み込み特性）がほぼ確定的な原因です。これはHTMLページ側の軽量化だけでは解消できない領域のため、次章の「静止画スナップショット方式」で回避する方針としました。

## 6. 黒画面の根本対策: 静止画スナップショット方式

サイネージのループが「HTMLページへのURL遷移」である限り、ブラウザの通常のページ読み込み特性による一瞬の空白（黒画面）は完全にはゼロにできません。そこで、**サイネージのループにはこのHTMLページを直接組み込まず、定期的に撮影した「静止画（PNG）」を他の画像スライド（バス・JR時刻表など）と同列で表示する**方式に変更します。画像スライド同士の切り替えでは黒画面が発生しないことを確認済みのため、この方式が最も確実です。

Xサーバーの管理者権限（FTP情報）が無い場合でも実施できるよう、**撮影した画像はXサーバーにアップロードせず、GitHub自身（GitHub Pages）に保存・公開**する構成にしています。必要なのは自分で作成したGitHubアカウントだけで、Xサーバー側の認証情報は一切使いません。

### 6.1 全体の流れ

1. GitHub Actions（無料のクラウド自動実行サービス）が、`*/30 * * * *`（30分ごと。`.github/workflows/weather-snapshot.yml`内のcron式で変更可能）のタイミングで自動起動
2. ヘッドレスブラウザ（Playwright/Chromium）で、Xサーバー上で公開中の`index.html?clock=off`を開き、天気データの取得完了を待ってスクリーンショットを撮影（`?clock=off`は時計を非表示にするURLパラメータ。静止画にすると時計が次の撮影まで止まって見えてしまうため）
3. 撮影したPNGを、同じリポジトリの`gh-pages`ブランチにpush（GitHubが自動発行する`GITHUB_TOKEN`のみ使用。Xサーバーの認証情報は不要）
4. GitHub Pagesがそのブランチを自動で公開し、`https://（GitHubユーザー名）.github.io/（リポジトリ名）/weather-snapshot.png`という固定URLで画像が取得できるようになる
5. サイネージのループ設定側で、天気予報ページのURLをこのGitHub PagesのURLに差し替える（バス・JR時刻表の画像と同じ扱いにする）

**index.html自体は今まで通りXサーバー上での公開を継続してください**（スクリーンショットの撮影対象として必要なため）。こちらは既存の「アップロードして公開する」だけの作業なので、管理者権限やFTP自動連携は不要です。

### 6.2 セットアップ手順

1. **GitHubアカウントを作成**（無料）: https://github.com/
2. このプロジェクトフォルダ（`index.html`・`bg-photo.jpg`・`.github/workflows/weather-snapshot.yml`一式）をGitHubの新規リポジトリにpush（プライベートリポジトリで問題ありません）
3. `.github/workflows/weather-snapshot.yml`内の`YOUR-DOMAIN-HERE`の部分を、Xサーバーで実際に公開しているドメイン（例: `example.com`）に書き換えてpush
4. GitHubリポジトリの `Actions` タブ→ `Weather Snapshot` を開き、`Run workflow`で手動実行し、エラーなく完了するか確認（初回実行が成功すると`gh-pages`ブランチが自動作成されます）
5. リポジトリの `Settings → Pages` を開き、`Source`を「Deploy from a branch」、ブランチを`gh-pages` / `/(root)`に設定して保存（`gh-pages`ブランチは手順4の実行後に選択肢として表示されます）
6. 数分待ってから`https://（GitHubユーザー名）.github.io/（リポジトリ名）/weather-snapshot.png`にブラウザでアクセスし、天気予報の画像が表示されるか確認
7. サイネージのループ設定側で、天気予報ページの参照先をこのURLに変更

### 6.3 補足・既知の注意点

- GitHub Actionsの`schedule`（cron）は指定時刻ちょうどに動くとは限らず、システムの混雑状況により数分程度前後することがあります（仕様上の制約）
- ワークフローは`force_orphan: true`で毎回1コミットに置き換える設定にしているため、リポジトリの容量が際限なく増えていくことはありません
- GitHub Pagesの反映には数十秒〜数分のタイムラグがあります。撮影〜公開まで含めても数分以内には最新化されます
- この方式に切り替えた場合、天気予報ページ自体（`index.html`）はもうサイネージのループには直接使われなくなりますが、スクリーンショット撮影対象として引き続きXサーバー上で公開しておく必要があります
- スクリーンショットの更新間隔（現在30分）とサイネージのループが一周する時間は無関係です。ループが何分で一周しても、画像自体は裏側で30分ごとに最新化され続けます
- リポジトリを**公開（Public）**にする場合、`index.html`・`HANDOFF.md`に含まれる情報（WeatherAPI.comのAPIキーなど）が誰でも閲覧できる状態になります。プライベートリポジトリのままにするか、公開する場合はAPIキーを含むファイルの扱いにご注意ください

## 7. 既知の制限・引き継ぎ時の注意点

1. **`WEATHERAPI_KEY`は初期値`"YOUR_API_KEY"`のプレースホルダーのままです。** 実際のAPIキーに置き換えるまでは全都市が取得失敗（フッターに「一部取得失敗 Error」）になります。キー設定後、実際のAPIレスポンスでの動作確認（アイコン表示・気温・降水確率が正しく出るか）をVS Code側の通常のブラウザで最初に必ず行ってください。
2. 24時間365日連続稼働させる場合、ブラウザのメモリ肥大化やページの固着を防ぐため、**定期的なページ全体リロード（例: 毎日深夜3時に`location.reload()`）を追加することを推奨**します。現状は未実装です。（6章の静止画スナップショット方式に切り替えた場合、サイネージ側はブラウザで直接このページを開き続けるわけではなくなるため、この項目の重要度は下がります）
3. `backdrop-filter`（ガラス風のぼかし効果）はモダンブラウザ（Chrome/Edge/Safari最新版）が前提です。古いブラウザや一部の組み込みブラウザでは効果が無効化され、パネルが単色表示になる場合がありますが、レイアウト自体は崩れません。
4. サイネージ端末のブラウザは、可能であれば **kioskモード**（例: `chrome --kiosk --incognito index.html`）での起動を推奨します。

## 8. 次の一手（任意の拡張案）

- 商用利用の場合のWeatherAPI.com有料プラン対応
- 定期フルリロードの実装（上記7-2）
- 風速・注意報・週間予報など追加情報の表示
- 実機（サイネージ端末）でのkioskモード起動スクリプトの用意

---

以上を踏まえて、VS Code + Claude Codeでの作業を継続してください。ご不明点があれば、この資料と`index.html` / `template.html`を渡せば経緯を共有できます。
